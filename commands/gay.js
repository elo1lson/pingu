const Discord = require('discord.js');
const lib = require('/c.js')
module.exports.run = async (client, message, args, prefix) => {
	a = lib.fun(args[0])
	console.log(a)
	message.reply("gay é você kkj")
}